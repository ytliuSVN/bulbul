{
  "compilerOptions": {
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "exclude": ["node_modules", "dist"],
  "scripts": {
    "deploy": "NODE_ENV=production npm run build && gh-pages -d dist"
  }
}
